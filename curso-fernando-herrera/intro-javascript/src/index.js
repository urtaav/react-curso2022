//Operador condiciona ternario

const active = true;

// let msg = '';

// if(active) {
//     msg = 'active'
//  }else{
//     msg = ' inactive';
//  }

// const msg = (active) ? 'Active' : ' Inactive';

const msg = active && 'Activo';
 console.log(msg);